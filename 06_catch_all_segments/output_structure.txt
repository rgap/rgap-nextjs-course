└── ./
    └── jsconfig.json
    └── next.config.mjs
    └── README.md
    └── package.json
    └── eslint.config.mjs
    └── public/
    └── src/
        └── app/
            └── layout.js
            └── page.js
            └── globals.css
            └── products/
                └── page.jsx
                └── [productId]/
                    └── page.jsx
                    └── reviews/
                        └── [reviewId]/
                            └── page.jsx
            └── about/
                └── page.jsx
            └── docs/
                └── [...slug]/
                    └── page.jsx
            └── blog/
                └── page.jsx
                └── first/
                    └── page.jsx
                └── second/
                    └── page.jsx
            └── profile/
                └── page.jsx

Contents of jsconfig.json:

{
  "compilerOptions": {
    "paths": {
      "@/*": ["./src/*"]
    }
  }
}


Contents of next.config.mjs:

/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
};

export default nextConfig;


Contents of README.md:

## Overview
`02_routing` demonstrates basic filesystem routing with the App Router. It contains a home page plus `/about` and `/profile` routes, each defined by their own `page.jsx` files to show how folders automatically become URLs.

## What You Practice
- Structuring `src/app` so every directory with a `page` file turns into a route.
- Creating sibling routes (`about`, `profile`) that share the same root `layout.js`.
- Using simple components and links to move between pages without additional config.

## How To Use It
Install dependencies and run `npm run dev`, then visit `/`, `/about`, and `/profile` to inspect how each file maps to a route. Try adding new directories under `src/app` or swapping the file extensions to TypeScript to reinforce how Next.js resolves files.


Contents of package.json:

{
  "name": "my-app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint"
  },
  "dependencies": {
    "next": "16.0.5",
    "react": "19.2.0",
    "react-dom": "19.2.0"
  },
  "devDependencies": {
    "eslint": "^9",
    "eslint-config-next": "16.0.5"
  }
}


Contents of eslint.config.mjs:

import { defineConfig, globalIgnores } from "eslint/config";
import nextVitals from "eslint-config-next/core-web-vitals";

const eslintConfig = defineConfig([
  ...nextVitals,
  // Override default ignores of eslint-config-next.
  globalIgnores([
    // Default ignores of eslint-config-next:
    ".next/**",
    "out/**",
    "build/**",
    "next-env.d.ts",
  ]),
]);

export default eslintConfig;


Contents of src/app/layout.js:

import "./globals.css";

export const metadata = {
  title: "Project 01 | Empty Next.js project",
  description: "Minimal starter without the stock Next.js demo content.",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}


Contents of src/app/page.js:

export default function Home() {
  return (
    //
    <main>Home page</main>
  );
}


Contents of src/app/globals.css:

:root {
  --background: #f8fafc;
  --foreground: #0f172a;
}


Contents of src/app/products/page.jsx:

export default function ProductList() {
  return (
    //
    <>
      <h1>Product list</h1>
      <h2>Product 1</h2>
      <h2>Product 2</h2>
      <h2>Product 3</h2>
    </>
  );
}


Contents of src/app/products/[productId]/page.jsx:

export default function ProductDetails({ params }) {
  return (
    //
    <div>ProductDetails page about product {params.productId}</div>
  );
}


Contents of src/app/products/[productId]/reviews/[reviewId]/page.jsx:

export default function ReviewDetail({ params }) {
  return (
    //
    <>
      <div>ReviewDetail page</div>
      <div>
        Review {params.reviewId} for product {params.productId}
      </div>
    </>
  );
}


Contents of src/app/about/page.jsx:

export default function About() {
  return (
    //
    <div>About page</div>
  );
}


Contents of src/app/docs/[...slug]/page.jsx:

export default function Docs({ params }) {
  if (params.slug.length === 2) {
    return (
      //
      <h1>
        Viewing docs for feature {params.slug[0]} and concept {params.slug[1]}
      </h1>
    );
  } else if (params.slug.length === 1) {
    return (
      //
      <h1>Viewing docs for feature {params.slug[0]}</h1>
    );
  }
  return (
    // The ...slug captures all urls segments
    <div>Docs page</div>
  );
}


Contents of src/app/blog/page.jsx:

export default function Blog() {
  return (
    //
    <div>Blog page</div>
  );
}


Contents of src/app/blog/first/page.jsx:

export default function FirstBlog() {
  return (
    //
    <div>FirstBlog post</div>
  );
}


Contents of src/app/blog/second/page.jsx:

export default function SecondBlog() {
  return (
    //
    <div>SecondBlog post</div>
  );
}


Contents of src/app/profile/page.jsx:

export default function Profile() {
  return (
    //
    <div>Profile page</div>
  );
}


